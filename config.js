/* ============================================
   PRIVATE ACCESS — change the password below
   Share this password only with Tayyaba.
   ============================================ */

window.GIFT_CONFIG = {
  // Comparison is case-insensitive
  accessPassword: "ForYouTayyaba",

  // Remember unlock on this device for 30 days
  rememberDays: 30,
};
